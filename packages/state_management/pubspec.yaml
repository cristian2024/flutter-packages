name: state_management
description: "A new Flutter package project."
resolution: workspace
version: 0.0.1
homepage:

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0

dev_dependencies:
  mocktail: ^1.0.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  
  
  

