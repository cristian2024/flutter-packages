name: flutter_packages

packages: 
  - packages/**
  - apps/**
  - flutter_packages/**

scripts:
  generate:
    run: melos exec -c 1 --depends-on build_runner -- dart run build_runner build
  test:
    run: melos exec -- flutter test
    description: Run all tests in every package